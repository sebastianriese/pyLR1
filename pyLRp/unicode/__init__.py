"""
Unicode handling for pyLRp

Currently contains the following:
* Support for character filters as input alphabet of the lexer

Should also have:
* Incremental unicode normalization (should be part of the runtime)
* UTF-8 match generator (as alternative 8bit input alphabet)
"""
